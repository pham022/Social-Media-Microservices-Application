spring:
  application:
    name: ComReactService
  datasource:
    url: jdbc:h2:mem:comreact
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 8084